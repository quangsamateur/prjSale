<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/container"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".lichtrichngua.MainActivity3">

    <androidx.appcompat.widget.Toolbar
        android:id="@+id/toolbar"
        app:menu="@menu/menu_main"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:background="#E91E63"
        android:gravity="center"
        app:title="Lập lịch tiêm chủng"></androidx.appcompat.widget.Toolbar>
    <TextView
        android:id="@+id/fabAddContact"
        android:layout_width="match_parent"
        android:layout_height="36dp"
        android:layout_marginTop="8dp"
        android:layout_below="@id/toolbar"
        android:paddingLeft="5dp"
        android:clickable="true"
        android:backgroundTint="#F3A7C0"
        android:background="@drawable/shape"
        android:textColor="@color/black"
        android:layout_marginRight="16dp"
        android:textSize="17dp"
        android:gravity="center"
        android:layout_marginLeft="16dp"
        android:text="Bạn có muốn thêm lập lịch?"
        />
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvContacts"
        android:layout_below="@id/fabAddContact"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginTop="15dp"
        tools:listitem="@layout/item_laplichtiem" />

</RelativeLayout>